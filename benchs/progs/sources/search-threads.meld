
type route edge(node, node).
type linear value(node, int).
type linear search(thread, list int).
type linear found(node, int).
type linear look(node).

//index search/2.

look(A).

look(A),
value(A, Val),
search(T, All),
Val in All
   -o found(A, Val),
      value(A, Val),
      search(T, lremove(All, Val)).

look(A),
value(A, Val)
   -o value(A, Val).
