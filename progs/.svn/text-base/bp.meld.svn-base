
type adjValues(catom, set_union float).
type linear value(catom, float).

extern float bpfn(float set).

!value(Y, S), adjValues(X, S) :-
	edge(X, Y), !value(Y, S).

!value(X, V) :- !value(X, _), adjValues(X, S), V = bpfn(S).

