
type fact1(node, int).
type fact2(node).
type fact3(node).

type scheduler resend.

fact1(A, 2).

fact2(A) :- fact1(A, _).

schedule(A, resend, -1) :- fact2(A).
//fact3(A) :- delay(fact2(A), resend).
fact3(A) :- resend(A), fact2(A).

schedule(A, resend, 3) :- fact2(A).



